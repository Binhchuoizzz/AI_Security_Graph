# ==========================================
# 1. CORE AI & AGENT (Lõi Suy luận LangGraph)
# ==========================================
langgraph>=0.3.34
langchain>=0.3.25
langchain-openai>=1.1.16
langchain-community>=0.3.21
pydantic>=2.10.0
langsmith>=0.8.0
langchain-text-splitters>=1.1.2

# ==========================================
# 2. RAG & KNOWLEDGE BASE (Tri thức AI)
# ==========================================
sentence-transformers>=3.3.0
faiss-cpu>=1.9.0
pyarrow>=18.0.0,<24.0.0
rank_bm25>=0.2.2

# ==========================================
# 3. DATA PROCESSING & STORAGE (Xử lý Log & DB)
# ==========================================
neo4j>=5.20.0
pandas>=2.2.3
numpy>=1.26.4

# ==========================================
# 3b. CỔNG ML TIER-1 (LightGBM + StandardScaler)
# ==========================================
# BẮT BUỘC cho runtime: `ml_lab/tier_2_model.pkl` được pickle từ sklearn + LightGBM.
# THIẾU 2 gói này thì pickle.load ném ModuleNotFoundError -> MLGateway._load_pipeline
# trả None -> Cổng ML TẮT ÂM THẦM (mọi ca leo thang dồn hết lên LLM) mà không có lỗi
# nào nổi lên. Đây chính là nguyên nhân CI đỏ liên tục từ 2026-07-17.
# Ghim phiên bản vì tương thích pickle nhạy với version của sklearn/LightGBM.
scikit-learn==1.7.2
lightgbm==4.6.0

# ==========================================
# 4. USER INTERFACE (Giao diện HITL)
# ==========================================
streamlit>=1.54.0
streamlit-authenticator>=0.4.2
streamlit-autorefresh>=1.0.1

# ==========================================
# 5. MLOPS & TRACKING (Vận hành & Theo dõi)
# ==========================================
mlflow>=3.11.1
python-dotenv>=1.0.1
PyYAML>=6.0.2

# ==========================================
# 6. TESTING & GUARDRAILS (Kiểm thử)
# ==========================================
pytest>=8.3.5
redis>=5.2.1
filelock>=3.25.2
# NOTE: drain3==0.9.11 is NOT listed here because it pins cachetools==4.2.1
# which conflicts with streamlit (needs cachetools>=5.5).
# Install separately: pip install drain3==0.9.11 --no-deps && pip install jsonpickle>=1.5.1
# drain3 works fine with modern cachetools despite its metadata constraint.

# ==========================================
# 6b. CODE QUALITY & FORMATTING (Linter / Formatter)
# ==========================================
ruff>=0.9.0

# ==========================================
# 7. CVE PATCHES (Transitive Dependency Overrides)
# ==========================================
Mako>=1.3.12
starlette>=1.3.1
urllib3>=2.7.0
# pip-audit 2026-06-12 — pin secure minimum cho dependency transitive có CVE:
aiohttp>=3.14.1          # CVE-2026-34993, CVE-2026-47265
authlib>=1.6.12          # PYSEC-2026-188
gitpython>=3.1.50        # CVE-2026-42215/42284/44244, GHSA-mv93-w799-cj2w
pyjwt>=2.13.0            # PYSEC-2026-175/177/178/179
langchain-core>=1.3.3    # CVE-2026-44843
# torch CVE-2025-3000: CHẤP NHẬN RỦI RO — chưa có bản vá upstream, không nằm trên
# bề mặt tấn công của SENTINEL (LLM phục vụ local, không nạp model untrusted). Xem commit 190cab7.

bleach>=6.4.0
click>=8.3.3
cryptography>=48.0.1
langchain>=1.3.9
langchain-classic>=1.0.7
langgraph-checkpoint>=4.1.1
langgraph-sdk>=0.3.15
langsmith>=0.8.18
msgpack>=1.2.1
pillow>=12.3.0
pydantic-settings>=2.14.2
setuptools>=83.0.0
tornado>=6.5.7
