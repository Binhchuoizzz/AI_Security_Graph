# Ablation Config D: MITRE-Only RAG
# Purpose: Isolate NIST SP 800-61r2 RAG contribution.
#          IF D ≈ F → NIST RAG adds nothing → drop Dual-RAG claim

tier1:
  enabled: true

guardrails:
  template_miner: true
  encapsulation: true

agent:
  enabled: true

rag:
  enabled_sources: ["mitre"]    # <-- KEY: MITRE only, no NIST

evaluation:
  tag: "ablation_D_mitre_only_rag"
