\chapter{Giới thiệu}
\label{ch:introduction}

Chương này thiết lập bối cảnh nghiên cứu cho SENTINEL, một kiến trúc nhận thức hai tầng tự động hóa phát hiện và phản hồi sự cố trong các Trung tâm Điều hành An ninh (SOC) hiện đại. Bằng việc phân tích nạn bội thực cảnh báo, sự mù lòa của hệ dựa-luật, và các giới hạn tính toán của Mô hình Ngôn ngữ Lớn (LLM), chương trình bày thiết kế và xác thực một pipeline phát hiện lai bảo mật, phác họa mục tiêu, câu hỏi nghiên cứu, phạm vi, và phương pháp định lượng.

\section{Bối cảnh và Động lực}
Mạng doanh nghiệp hiện đại đã trở nên cloud-native và xốp, mở rộng mạnh bề mặt tấn công. Kẻ tấn công trải từ malware phổ thông tới các chiến dịch đa pha tinh vi~\cite{mlids2016}, nhưng đều hội tụ về một nút thắt: khối lượng telemetry khổng lồ mà người phòng thủ phải sàng lọc. Các SOC hợp nhất log từ NGFW, IDS/IPS, EDR vào nền tảng SIEM tập trung~\cite{siem2021} để chuẩn hóa và liên kết. Việc lọc tuyến đầu như vậy (IDS/IPS tốc độ dây, thu nạp ở rìa) là tất định và cứng nhắc: nó cắt khối lượng nhưng không thể suy luận về một cảnh báo mơ hồ hay giải thích một phán quyết, nên tốc độ telemetry vẫn tạo ra một ``Nghịch lý Dữ liệu Lớn''~\cite{bigdata2019} đẩy phán đoán ngữ cảnh về lại analyst con người. Điều này biểu hiện thành khủng hoảng \emph{mệt mỏi cảnh báo} nghiêm trọng: analyst bị ngập bởi thông báo độ-tin-cậy-thấp, phần lớn báo giả~\cite{alertfatigue2022}, làm tăng mạnh nguy cơ bỏ sót xâm nhập thật.

Hai khoảng trống nữa trầm trọng thêm. Thứ nhất, các bộ lọc neo-chữ-ký~\cite{mlids2016} mù cấu trúc với biến thể mới (zero-day, malware đa hình) né luật tĩnh bằng thay đổi payload nhỏ, bộc lộ nhu cầu một tín hiệu ngoại-lai-thống-kê độc lập chữ ký. Thứ hai, lấy mẫu log vì chi phí cắt đứt liên kết thời gian cần để tái dựng chuỗi chậm-âm-thầm~\cite{apt_stealth2021}. SOAR thông thường cũng không lấp khoảng trống suy luận---nó chạy playbook cứng định sẵn không thể suy luận hay giải thích cảnh báo ngoài kịch bản~\cite{soar2025}.

LLM hứa hẹn đúng khả năng suy luận ngữ cảnh còn thiếu này, nhưng dùng trực tiếp trong SOC vấp một nghịch lý tích hợp. Độ trễ suy luận của chúng quá lớn cho log tốc độ cao do chi phí bậc hai của self-attention~\cite{vaswani2017}; giao diện ngôn ngữ tự nhiên phơi chúng trước injection nền-log, nơi payload nhúng trong trường như User-Agent chiếm quyền suy luận nếu không cách ly nghiêm ngặt~\cite{promptinjection2023}; và trong môi trường air-gapped hay Zero-Trust~\cite{nist80207} telemetry không thể rời đi tới mô hình đám mây, nên tầng suy luận phải chạy cục bộ. Để giải quyết, SENTINEL đề xuất mô hình hai tầng: một bộ lọc Tầng 1 phi trạng thái ở tốc độ đường truyền, một cổng học giảm tải phần lớn ca leo thang chắc chắn, và một tầng AI Tác tử Tầng 2 an toàn, host cục bộ thực hiện suy luận cách ly nhận thức. Như sản phẩm phụ, thiết kế có trạng thái cũng bảo toàn chuỗi chậm-âm-thầm và gắn cờ ngoại lai không chữ ký---coi là năng lực nền tảng, không phải kết quả trung tâm.

\section{Mục tiêu Nghiên cứu}
Mục tiêu tổng quát là thiết kế, triển khai và đánh giá định lượng kiến trúc nhận thức hai tầng SENTINEL kết hợp lọc thống kê độ-trễ-thấp với suy luận tác tử an toàn, nhận-thức-ngữ-cảnh. Ba mục tiêu cụ thể: (1)~xây dựng một bộ lọc tốc độ đường truyền triển khai thuật toán Welford trực tuyến cho phương sai và Z-score lăn $O(1)$, loại lưu lượng lành tính ngay tầng thu nạp; (2)~thiết kế một tác tử nhận thức có trạng thái trên LangGraph và một LLM lượng tử hóa cục bộ, tích hợp rào chắn đầu-vào-đầu-ra vô hiệu các vector đối kháng nhắm hệ tích-hợp-LLM; và (3)~dựng một hệ nối-nền tăng-cường-truy-xuất kép hợp nhất MITRE ATT\&CK với NIST SP~800-61r2 để nối nền quyết định và giảm ảo giác.

\section{Câu hỏi Nghiên cứu}
Ba câu hỏi dẫn dắt kiểm chứng thực nghiệm. \textbf{RQ1:} làm thế nào thiết kế một cơ chế lọc thời gian thực trên baseline thống kê trực tuyến để chạy ở tốc độ đường truyền và giảm mệt mỏi cảnh báo trong khi bảo toàn chuỗi sự kiện thời gian của chiến dịch APT? \textbf{RQ2:} bằng phương pháp kiến trúc nào có thể tích hợp LLM cục bộ vào pipeline phản hồi tự động, với đóng gói đầu-vào-đầu-ra an toàn, để giảm độ trễ trong khi vô hiệu các vector tấn công đối kháng? \textbf{RQ3:} hiệu quả thực nghiệm nào đạt được khi tăng cường bộ nhớ tác tử bằng một hệ truy-xuất-tri-thức kép dựa trên Reciprocal Rank Fusion, đo bằng độ chính xác phân loại và giảm ảo giác?

\section{Phạm vi và Đối tượng Nghiên cứu}
\textbf{Đối tượng nghiên cứu:} chính là kiến trúc nhận thức hai tầng---cơ chế mà một bộ lọc Tầng 1 tất định phi trạng thái ở tốc độ đường truyền được kết hợp với một tầng tác tử Tầng 2 có trạng thái host cục bộ để tự động hóa phát hiện và phản hồi SOC. Điều này bao hàm các cấu phần: telemetry flow tốc độ cao ở OSI Lớp~3--4 (five-tuple NetFlow và đặc trưng flow CICFlowMeter, với metadata PCAP thay vì soi payload sâu); điều phối tác tử có trạng thái (LangGraph); LLM lượng tử hóa cục bộ cho suy luận air-gapped; và các vector đối kháng injection nền-log, buôn lậu delimiter, và đầu độc kho tri thức. \textbf{Phạm vi:} đánh giá giới hạn ở CSE-CIC-IDS2018~\cite{ids2018} cho tấn công tổng quát và DAPT2020~\cite{dapt2020} cho APT đa giai đoạn, với zero-day mô phỏng bằng cách tiêm độ lệch Welford cực trị vào luồng benign. Mô hình là \textit{Gemma-2-9B-IT} cục bộ lượng tử hóa Q6\_K qua \texttt{llama.cpp} trên một GPU phổ thông, đảm bảo chủ quyền dữ liệu. Hành động tự chủ giới hạn ở sinh chính sách tường lửa, báo cáo, và pháp y, loại trừ ngắt kết nối vật lý tự động.

\section{Phương pháp Nghiên cứu}
Luận văn theo hướng Nghiên cứu Khoa học Thiết kế dưới lập luận suy diễn, định lượng: nó tạo tri thức bằng cách xây một artifact hoạt động (SENTINEL) và đánh giá nó trước một bài toán vận hành thực, chốt mọi khẳng định bằng đo lường. Nó lập luận theo ba cách: thống kê (tách lưu lượng thường khỏi dị biệt), logic (cấu trúc quy trình quyết định của tác tử), và đối kháng (thử độ bền trước thao túng). Cụ thể, hệ được triển khai như một bản mẫu hai tầng, được thử qua các thí nghiệm đầu-cuối phát lại dữ liệu lưu trữ và lưu lượng trực tiếp dưới đầu vào đối kháng, và được đánh giá trên khung năm chiều (độ chính xác, hiệu suất, bảo mật, tính giải thích, tính toàn vẹn). Ablation có kiểm soát cô lập đóng góp từng cấu phần, và kiểm định phi tham số xác nhận các khác biệt là có ý nghĩa. Nghiên cứu dùng dữ liệu định lượng (đo lường số và đầu ra như độ chính xác, độ trễ) và dữ liệu định tính (giải thích ngôn ngữ tự nhiên của mô hình), cái sau được chấm qua một khung tự động~\cite{ragas2023} và một trọng tài LLM chéo họ độc lập để chất lượng suy luận cũng đánh giá được trên cơ sở so sánh.

\section{Đóng góp của Luận văn}
Đóng góp gồm ba phần, mỗi phần nêu ở đúng phạm vi bằng chứng cho phép. \textbf{Về kiến trúc}, công trình kiểm chứng một pipeline hai tầng khiến một LLM host cục bộ nhiều giây khả thi vận hành trong SOC. Đóng góp không phải một tiền lọc cô lập mà là sự \emph{kết hợp}: một cổng thống kê $O(1)$ phi trạng thái và một cổng học giảm-tải hấp thụ lưu lượng lành tính và phân-loại-chắc-chắn ở tốc độ đường truyền và bảo vệ mạng tất định, trong khi tầng nhận thức đắt đỏ được gọi bất đồng bộ và chỉ trên thiểu số mơ hồ. \textbf{Về bảo mật AI}, nó đóng góp một phòng thủ tất định, cấu trúc chống injection nền-log---Đóng gói Dữ liệu Phân định, một rào chắn đồng thuận Tầng 1/Tầng 2, và một chuỗi audit HMAC---mà bảo đảm lõi đúng theo cấu trúc chứ không theo huấn luyện: một rào chắn đồng thuận luật-cố-định không thể bị thuyết phục hạ cấp, dù payload đã gặp hay chưa. \textbf{Về điều phối}, nó thay Tự động hóa Runbook cứng bằng một tác tử có trạng thái mà luồng là một đồ thị trạng thái không-chu-trình, không-công-cụ trên một mô hình lượng tử hóa cục bộ, cho một tầng vừa thích ứng vừa tái lập và kiểm toán pháp y được dưới ràng buộc chủ quyền dữ liệu. Hai năng lực nữa mà kiến trúc cho phép---escalation dị biệt không chữ ký và liên kết đa ngày nổi lên---được nêu như bằng chứng khái niệm giới hạn trung thực, với việc trưởng thành sản xuất dời sang tương lai.

\section{Cấu trúc của Luận văn}
Chương~\ref{ch:theoretical_background} thiết lập nền tảng lý thuyết (lượng tử hóa LLM, mô hình đồ thị trạng thái, RAG, thống kê trực tuyến) và tổng quan nghiên cứu tác-tử-an-ninh liên quan. Chương~\ref{ch:system_design_implementation} trình bày thiết kế hệ thống: bộ lọc hai tầng, Cổng ML, FSM tác tử có trạng thái, truy xuất lai, rào chắn, dashboard, và triển khai. Chương~\ref{ch:experiments_evaluation} trình bày thí nghiệm, ablation, và kiểm chứng thống kê. Chương~\ref{ch:conclusion} kết luận với các phát hiện, giới hạn, và hướng tương lai.
