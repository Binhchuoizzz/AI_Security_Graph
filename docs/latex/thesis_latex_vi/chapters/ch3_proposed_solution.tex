\chapter{Kiến trúc SENTINEL Đề xuất}
\label{ch:proposed_solution}

Chương này trình bày chi tiết thiết kế kỹ thuật và mô hình hóa toán học của kiến trúc nhận thức hai tầng SENTINEL. Được cấu trúc như một quy trình phát hiện đe dọa và phản hồi sự cố lai (hybrid), hệ thống này được thiết kế nhằm san lấp khoảng cách hiệu năng giữa xử lý gói tin mạng ở tốc độ đường truyền (wire-speed) và khả năng suy luận tiêu tốn tài nguyên của Mô hình Ngôn ngữ Lớn (LLM), đồng thời thiết lập các rào chắn nhận thức (cognitive guardrails) mạnh mẽ để cách ly LLM khỏi các hành vi thao túng đối kháng. Nội dung chương tập trung phân tích sơ đồ khối hệ thống tổng quát, các thuật toán tiền lọc thống kê trực tuyến, quy trình điều phối nhận thức dựa trên đồ thị trạng thái, cơ chế truy xuất kép Dual-RAG, và các tầng bảo vệ toàn vẹn dữ liệu bằng mật mã học.

\section{Tổng quan về Kiến trúc Nhận thức Hai tầng}

Để bảo vệ môi trường mạng doanh nghiệp mà không phải đánh đổi bằng độ trễ tính toán quá lớn, hệ thống SENTINEL chia quy trình phát hiện và phản hồi mối đe dọa thành hai tầng hoạt động chuyên biệt và riêng biệt. Sự phân tách trách nhiệm kiến trúc (architectural separation of concerns) này được minh họa trực quan thông qua sơ đồ khối hệ thống tổng thể trong Hình~\ref{fig:soc_ai_concept}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{images/soc_ai_concept.png}
    \caption{Sơ đồ kiến trúc tổng thể của hệ thống SENTINEL, minh họa luồng dữ liệu hai tầng và các thành phần cấu trúc.}
    \label{fig:soc_ai_concept}
\end{figure}

Tầng thứ nhất, được ký hiệu là Tầng 1 (Tier-1), hoạt động như một tầng vận chuyển và lọc dữ liệu phi trạng thái (stateless) với vận tốc cao, được bố trí trực tiếp tại ranh giới mạng. Dữ liệu đo lường từ tường lửa, các cảm biến mạng và tác tử trên máy chủ (host agent) được nạp vào dưới dạng dòng gói tin bắt được (packet capture) hoặc nhật ký luồng (flow logs). Tầng 1 vận hành ở tốc độ đường truyền, tính toán các độ lệch thống kê theo thời gian thực trên dữ liệu đo lường đầu vào để ngay lập tức loại bỏ hoặc ghi nhật ký các gói tin lành tính, đồng thời chuyển tiếp các nhật ký bất thường lên Tầng 2. Ngược lại, Tầng 2 (Tier-2) được thiết kế như một tầng nhận thức có trạng thái (stateful) với khả năng suy luận chuyên sâu. Được điều phối bởi một tác tử AI tự trị thông qua LangGraph \cite{langgraph2024}, Tầng 2 tiếp nhận các cảnh báo được leo thang, truy xuất tri thức an ninh mạng chuyên biệt, xâu chuỗi ngữ cảnh lịch sử, và đưa ra các quyết định phản hồi có cấu trúc (ví dụ: đề xuất cập nhật cấu hình tường lửa). Thiết kế lai này giải quyết triệt để nút thắt độ trễ của LLM bằng cách giảm tải tới 95\% lưu lượng lành tính ngay tại Tầng 1, chỉ dành riêng tài nguyên suy luận LLM chuyên sâu cho các cuộc điều tra mối đe dọa phức tạp.

\section{Tầng 1: Bộ lọc Lưu lượng Mạng Thời gian thực (RuleEngine và Welford)}

Bộ lọc Tầng 1 chịu trách nhiệm thu nạp và xử lý theo thời gian thực luồng nhật ký tốc độ cao. Để quản lý trạng thái kết nối mà không gây ra nút thắt cổ chai về đọc/ghi đĩa (disk I/O), bộ lọc tích hợp một trình quản lý đường cơ sở phiên tạm thời (temporal session baseline manager) để ánh xạ các địa chỉ IP nguồn đang hoạt động. Đối với mỗi phiên IP hoạt động, hệ thống tính toán các đặc trưng lưu lượng bằng thuật toán trực tuyến Welford \cite{welford1962}. Công thức toán học của thuật toán Welford tính toán giá trị trung bình động (running mean) $\mu_k$ và tổng bình phương các độ lệch (sum of squared differences) $M_{2,k}$ trên một luồng liên tục các thuộc tính số của nhật ký (chẳng hạn như thời gian luồng, số lượng gói tin, hoặc kích thước byte) tại bước thứ $k$:

\begin{equation}
    \mu_k = \mu_{k-1} + \frac{x_k - \mu_{k-1}}{k}
\end{equation}

\begin{equation}
    M_{2,k} = M_{2,k-1} + (x_k - \mu_{k-1})(x_k - \mu_k)
\end{equation}

Từ các số liệu thống kê lũy kế này, phương sai cuộn (rolling variance) $\sigma_k^2$ được tính như sau:

\begin{equation}
    \sigma_k^2 = \frac{M_{2,k}}{k}
\end{equation}

Đối với mỗi bản ghi nhật ký đầu vào, hệ thống tính toán điểm Z-Score $Z$ của các thuộc tính lưu lượng đặc trưng:

\begin{equation}
    Z = \frac{x_k - \mu_k}{\sigma_k}
\end{equation}

Nếu điểm Z-Score vượt quá một ngưỡng tới hạn (được xác định trong cấu hình, ví dụ: $\alpha > 3.5\sigma$), gói tin sẽ được gắn nhãn hành động `ESCALATE` (leo thang) và chuyển tiếp lên Tầng 2. Để duy trì chuỗi sự kiện theo thời gian của các chiến dịch tấn công âm thầm và chậm rãi (low-and-slow), hệ thống tiếp tục cập nhật đường cơ sở của các phiên. Song song với các kiểm tra thống kê dựa trên thuật toán Welford, Tầng 1 vận hành một bộ máy luật tĩnh (\texttt{RuleEngine}) thực hiện so khớp mẫu chữ ký nhanh bằng biểu thức chính quy (regex) để ngăn chặn tức thì các mẫu câu lệnh đã biết (như cú pháp tấn công SQL Injection) và áp dụng các hành động tĩnh `DROP` (loại bỏ) hoặc `WARN` (cảnh báo) mà không cần đi qua Tầng 2.

\section{Tầng 2: Tác tử AI Nhận thức LangGraph}

Các luồng bất thường được leo thang sẽ được tiếp nhận bởi công cụ nhận thức Tầng 2, được mô hình hóa dưới dạng Đồ thị có hướng không chu trình (Directed Acyclic Graph) hoạt động như một Máy trạng thái hữu hạn (Finite State Machine - FSM) có trạng thái bằng cách sử dụng LangGraph \cite{langgraph2024}. Dữ liệu phiên và ngữ cảnh suy luận được lưu giữ bên trong một đối tượng trạng thái trung tâm, \texttt{SentinelState}. Quy trình làm việc của tác tử bao gồm ba nút chính: nút phân tích (analyze), nút truy xuất (retrieve), và nút giải quyết (resolve). Tại nút phân tích, tác tử đánh giá ngữ cảnh nhật ký được leo thang bằng cách sử dụng LLM lượng hóa cục bộ để phân loại rủi ro ban đầu. Nếu tác tử phát hiện sự thiếu hụt kiến thức chuyên môn (như một CVE chưa biết hoặc một chữ ký không quen thuộc), nó sẽ kích hoạt quá trình chuyển hướng điều kiện đến nút truy xuất. Tại nút truy xuất, hệ thống thực hiện truy vấn cơ sở tri thức lai kết hợp vector và từ khóa. Các trạng thái định tuyến và chuyển dịch được trình bày chi tiết trong Bảng~\ref{tab:agent_fsm_transitions}.

\begin{table}[H]
    \centering
    \caption{Ma trận chuyển trạng thái của FSM Tác tử Nhận thức Tầng 2.}
    \label{tab:agent_fsm_transitions}
    \begin{tabular}{llll}
        \toprule
        \textbf{Trạng thái hiện tại} & \textbf{Đầu vào / Điều kiện} & \textbf{Trạng thái tiếp theo} & \textbf{Hành động đầu ra} \\
        \midrule
        \texttt{INIT} & Nhập cảnh báo & \texttt{ANALYZE} & Tải ngữ cảnh phiên \\
        \texttt{ANALYZE} & Độ tin cậy cao / Đe dọa đã biết & \texttt{RESOLVE} & Phân loại mối đe dọa \\
        \texttt{ANALYZE} & Độ tin cậy thấp / Thiếu thông tin & \texttt{RETRIEVE} & Câu truy vấn tìm kiếm \\
        \texttt{RETRIEVE} & Tri thức đã được truy xuất & \texttt{ANALYZE} & Bổ sung ngữ cảnh vào Trạng thái \\
        \texttt{RESOLVE} & Độ tin cậy $\ge 0.7$ & \texttt{EXECUTE} & Đề xuất phản hồi tương ứng \\
        \texttt{RESOLVE} & Độ tin cậy $< 0.7$ & \texttt{HITL} & Leo thang tới Chuyên viên SOC \\
        \bottomrule
    \end{tabular}
\end{table}

Sau khi quá trình truy xuất hoàn tất, luồng xử lý sẽ quay vòng lại nút phân tích để tái đánh giá các bản ghi nhật ký cùng với các thông tin ngữ cảnh được bổ sung. Chu kỳ lặp này giúp ngăn chặn hiện tượng ảo giác (hallucination) bằng cách ràng buộc suy luận của LLM dựa trên các tài liệu đã được xác thực. Khi độ tin cậy đạt đến ngưỡng quy định, tác tử chuyển sang nút giải quyết để tạo một cấu trúc dữ liệu JSON chi tiết chứa phân loại mối đe dọa, độ tin cậy và các quy tắc ngăn chặn được đề xuất. Nếu độ tin cậy dưới ngưỡng, tác tử sẽ định tuyến cảnh báo đến hàng đợi phê duyệt của con người (Human-in-the-Loop - HITL).

\section{Cơ chế Sinh Tăng cường Truy xuất Kép (Dual-RAG) và Bộ nhớ Ngữ nghĩa}

Cơ chế Dual-RAG định hình các quyết định của tác tử bằng cách truy xuất ngữ cảnh từ hai cơ sở dữ liệu chuyên ngành thống nhất: cơ sở tri thức MITRE ATT\&CK dùng để ánh xạ các hành vi, chiến thuật và kỹ thuật đe dọa (TTPs), và kho lưu trữ kịch bản NIST SP 800-61r2 cung cấp các quy trình xử lý sự cố tiêu chuẩn \cite{nist80061}. Thay vì chỉ dựa vào khớp từ khóa đơn thuần hoặc các vector ngữ nghĩa dày đặc một cách độc lập \cite{rag2020}, quá trình truy xuất sử dụng một chiến lược tìm kiếm lai (hybrid search). Bảng so sánh giữa các thành phần tìm kiếm dày đặc (dense) và thưa thớt (sparse) được chi tiết hóa trong Bảng~\ref{tab:search_comparison}.

\begin{table}[H]
    \centering
    \caption{So sánh các mô hình truy xuất trong chiến lược tìm kiếm lai.}
    \label{tab:search_comparison}
    \begin{tabular}{p{0.25\textwidth}p{0.32\textwidth}p{0.33\textwidth}}
        \toprule
        \textbf{Khía cạnh} & \textbf{Tìm kiếm Vector Dày đặc (FAISS)} & \textbf{Tìm kiếm Từ khóa Thưa thớt (BM25)} \\
        \midrule
        \textbf{Mô hình nền tảng} & all-MiniLM-L6-v2 Embeddings & Trọng số từ khóa BM25 \\
        \midrule
        \textbf{Logic so khớp} & Độ tương đồng Cosine ngữ nghĩa \cite{faiss2019} & Tần suất từ - Tần suất nghịch đảo tài liệu (TF-IDF) \cite{bm25_2009} \\
        \midrule
        \textbf{Ưu điểm} & Bắt được các từ đồng nghĩa, khái niệm và ngữ nghĩa trừu tượng & So khớp chính xác các định danh kỹ thuật (CVEs, IPs, số cổng) \\
        \midrule
        \textbf{Hạn chế} & Yếu khi khớp chính xác các từ khóa băm kỹ thuật & Bị mù trước các từ đồng nghĩa ngữ nghĩa và ngữ cảnh khái niệm \\
        \bottomrule
    \end{tabular}
\end{table}

Điểm số từ các luồng xử lý dày đặc (FAISS) và thưa thớt (BM25) được tổng hợp lại bằng thuật toán Tổng hợp xếp hạng nghịch đảo (Reciprocal Rank Fusion - RRF) \cite{rrf2009}, tính toán một điểm số thống nhất $R$ cho mỗi tài liệu $d$ trên tập hợp các lượt tìm kiếm $M$:

\begin{equation}
    R(d) = \sum_{m \in M} \frac{1}{k + r_m(d)}
\end{equation}

trong đó $r_m(d)$ là thứ hạng của tài liệu $d$ trong lượt tìm kiếm $m$, và $k$ là một hằng số tỷ lệ (thường chọn $k=60$). Để vượt qua nút thắt độ trễ của LLM, một mô-đun Bộ đệm Ngữ nghĩa (Semantic Cache) được triển khai phía trước tác tử. Bộ đệm này tính toán độ tương đồng Cosine giữa vector đặc trưng (embedding) của truy vấn mới và các thực thể truy vấn lịch sử. Nếu độ tương đồng vượt quá $\theta \ge 0.95$, hệ thống sẽ trả về ngay kết quả đã lưu trong bộ đệm, rút ngắn thời gian xử lý từ vài giây xuống còn mili giây.

\section{Bộ nhớ Đe dọa và Liên kết Chiến dịch APT}

Các tác nhân đe dọa tinh vi thường chia nhỏ chuỗi tấn công của chúng thành các sự kiện có mức độ nghiêm trọng thấp và phân tán trên các khoảng thời gian dài nhằm vượt qua các bộ lọc gói tin phi trạng thái. Để vạch trần các chiến dịch tấn công âm thầm và chậm rãi này \cite{apt_stealth2021}, kiến trúc SENTINEL tích hợp một mô-đun Bộ nhớ Đe dọa (Threat Memory) bền vững, được triển khai dưới dạng cơ sở dữ liệu SQLite cục bộ. Mô-đun này được xây dựng trên các lược đồ (schema) chuyên dụng để theo dõi các cảnh báo lịch sử, điểm uy tín của các thực thể, và chuỗi tấn công theo thời gian. Chi tiết về các lược đồ cơ sở dữ liệu được trình bày trong Bảng~\ref{tab:threat_memory_schema}.

\begin{table}[H]
    \centering
    \caption{Thông số kỹ thuật lược đồ cơ sở dữ liệu Bộ nhớ Đe dọa.}
    \label{tab:threat_memory_schema}
    \begin{tabular}{p{0.25\textwidth}p{0.3\textwidth}p{0.35\textwidth}}
        \toprule
        \textbf{Tên bảng} & \textbf{Trường khóa chính / thuộc tính} & \textbf{Mục đích chức năng} \\
        \midrule
        \texttt{ip\_reputation} & \texttt{ip\_address} (PK), \texttt{score}, \texttt{last\_seen}, \texttt{status} & Theo dõi điểm uy tín (0-100) và vòng đời hoạt động của các IP nguồn. \\
        \midrule
        \texttt{known\_entities} & \texttt{entity\_name} (PK), \texttt{ip\_address}, \texttt{role} & Khởi tạo trước các dịch vụ nội bộ đã được xác minh (như AD, DNS) để giảm thiểu cảnh báo giả. \\
        \midrule
        \texttt{threat\_events} & \texttt{event\_id} (PK), \texttt{ip\_address}, \texttt{timestamp}, \texttt{alert\_type}, \texttt{severity} & Lưu trữ chuỗi các cảnh báo đã được leo thang theo trình tự thời gian phục vụ mục đích liên kết. \\
        \bottomrule
    \end{tabular}
\end{table}

Điểm uy tín của một địa chỉ IP sẽ tăng lên dựa trên mức độ nghiêm trọng của các cảnh báo mà nó kích hoạt, giới hạn tối đa là 100. Ngược lại, một hàm suy giảm (decay function) định kỳ chạy để giảm dần điểm uy tín của các IP không còn hoạt động:

\begin{equation}
    S_{new} = S_{old} \times (1 - \lambda)^{t}
\end{equation}

trong đó $\lambda$ là tỷ lệ suy giảm và $t$ là thời gian không hoạt động đã trôi qua. Thuật toán liên kết APT sẽ quét bảng \texttt{threat\_events}. Nếu phát hiện một IP nguồn kích hoạt các dị thường trong nhiều ngày khác nhau hoặc trên nhiều giai đoạn khác nhau của ma trận MITRE ATT\&CK, hệ thống sẽ tự động nâng mức phân loại mối đe dọa lên nghiêm trọng cao, kích hoạt các biện pháp chặn phòng thủ.

\section{Tầng Rào chắn Bảo mật AI (Guardrails)}

Do các LLM dễ bị tấn công chiếm quyền điều khiển chỉ thị (instruction hijacking / prompt injection), hệ thống SENTINEL triển khai một tầng Rào chắn Bảo mật AI (AI Security Guardrails) nhiều lớp nhằm cách ly công cụ suy luận khỏi các dữ liệu đầu vào không đáng tin cậy \cite{promptinjection2023}. Cơ chế phòng thủ cốt lõi dựa trên kỹ thuật Đóng gói Dữ liệu Phân tách (Delimited Data Encapsulation). Đối với mỗi phiên làm việc, hệ thống tự động tạo ra một mã token thập lục phân ngẫu nhiên bảo mật bằng mật mã (ví dụ: \texttt{<|DATA\_8F3E|>}). Phần dữ liệu nhật ký thô chưa được kiểm chứng sẽ được bao bọc bên trong các thẻ phân tách này trong khuôn mẫu prompt, chỉ thị cho LLM rằng bất kỳ mã lệnh nào xuất hiện bên trong ranh giới này đều phải được xử lý nghiêm ngặt dưới dạng dữ liệu tĩnh thụ động thay vì các chỉ thị thực thi.

Để chống lại các cuộc tấn công Từ chối dịch vụ Token (Token DoS) và tràn ngữ cảnh, hệ thống tích hợp một trình Khai thác Mẫu Nhật ký (Log Template Miner) dựa trên thuật toán Drain3. Trình khai thác này nhóm các bản ghi nhật ký tương đồng thành các khuôn mẫu chung, loại bỏ các tham số ngẫu nhiên (chẳng hạn như mốc thời gian động hoặc ID luồng). Bộ Quản lý Ngân sách Token (Token Budget Manager) sau đó phân bổ tối đa 60\% dung lượng ngữ cảnh cho các khuôn mẫu đã nén và 40\% còn lại cho các điểm dị biệt có entropy cao. Cuối cùng, một Bộ lọc Làm sạch Đầu ra (Output Sanitization Filter) áp dụng cơ chế xác thực lược đồ JSON nghiêm ngặt, kiểm chứng câu trả lời của LLM chỉ chứa các trường được phép, đồng thời loại bỏ các thẻ hình ảnh markdown, kịch bản SVG, và các vector vượt rào mã hóa thập lục phân \cite{watchtower2026}.

\section{Toàn vẹn Dữ liệu và Bảo vệ bằng Chuỗi HMAC}

Để bảo vệ cơ sở dữ liệu nhật ký bảo mật trước các hành vi giả mạo bởi người nội bộ độc hại hoặc những kẻ tấn công giành được quyền truy cập vào hệ thống tệp tin, hệ thống triển khai kỹ thuật chuỗi liên kết log bằng HMAC (HMAC log chaining). Mỗi bản ghi nhật ký an ninh được liên kết mật mã với bản ghi liền trước nó, tạo thành một chuỗi vết kiểm toán tương tự cấu trúc blockchain nội bộ. Đối với bản ghi thứ $i$, mã băm bản ghi $H_i$ được tính toán như sau:

\begin{equation}
    H_i = \text{HMAC-SHA256}(D_i \parallel H_{i-1}, K)
\end{equation}

trong đó $D_i$ là dữ liệu tải tin của bản ghi hiện tại, $H_{i-1}$ là mã băm của bản ghi liền trước, và $K$ là khóa bí mật HMAC được lưu trữ an toàn. Nếu kẻ tấn công cố tình thay đổi nhãn nhật ký hoặc xóa một bản ghi, chuỗi băm sẽ bị đứt gãy và các quy trình kiểm tra xác thực chạy trên bảng điều khiển sẽ lập tức gắn cờ phát hiện hành vi giả mạo. Ngoài ra, hệ thống cũng bảo vệ cơ sở dữ liệu vector RAG trước các cuộc tấn công đầu độc ngữ nghĩa (semantic poisoning). Các tệp tin cơ sở dữ liệu vector được giám sát chặt chẽ bởi một trình quản lý mã checksum chuyên dụng thực hiện tính toán và so sánh giá trị băm SHA-256 của các tệp tin trước khi nạp chúng vào bộ nhớ, đảm bảo không có tài liệu độc hại nào bị tiêm vào bộ nhớ truy xuất của tác tử.

Tóm lại, Chương 3 đã thiết lập cấu trúc thiết kế và các khung toán học cốt lõi của kiến trúc SENTINEL, cân bằng giữa khả năng lọc mạng ở tốc độ đường truyền (Tầng 1) và suy luận có trạng thái, bảo mật của tác tử AI (Tầng 2) được củng cố bởi tính toàn vẹn mật mã vững chắc. Chương 4 sẽ mô tả quá trình hiện thực hóa kiến trúc này thành mã nguồn thực tế.
